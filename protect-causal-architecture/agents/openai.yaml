interface:
  display_name: "Protect Causal Architecture"
  short_description: "Protect disputed authority without blocking work"
  default_prompt: "Use $protect-causal-architecture to preserve a genuinely disputed causal invariant while keeping already-authorized implementation moving."
