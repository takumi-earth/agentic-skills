interface:
  display_name: "Persist Pending Creation Batches"
  short_description: "Persist complete candidate batches once"
  default_prompt: "Use $persist-pending-creation-batches to apply the variant-002-write-ahead-batch-manifest pending design with explicit evidence and authority boundaries."
