interface:
  display_name: "Persist Pending Creation Batches"
  short_description: "Compare a rejected checkpoint strategy"
  default_prompt: "Use $persist-pending-creation-batches only to compare the rejected per-candidate checkpoint strategy; do not execute it for an active creation batch."
policy:
  allow_implicit_invocation: false
