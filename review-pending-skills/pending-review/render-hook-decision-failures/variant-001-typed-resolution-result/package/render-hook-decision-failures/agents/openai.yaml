interface:
  display_name: "Render Hook Decision Failures"
  short_description: "Render exact actionable hook failures"
  default_prompt: "Use $render-hook-decision-failures to render this typed decision as one safe Codex PostToolUse context envelope."
