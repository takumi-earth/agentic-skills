interface:
  display_name: "Render Hook Decision Failures"
  short_description: "Render exact actionable hook failures"
  default_prompt: "Use $render-hook-decision-failures to apply the variant-002-structured-failure-exceptions pending design with explicit evidence and authority boundaries."
