interface:
  display_name: "Bootstrap Skills Ref Validation"
  short_description: "Resolve and run the pinned skill validator"
  default_prompt: "Use $bootstrap-skills-ref-validation to invoke Python helpers correctly and report canonical validator outcomes separately."
