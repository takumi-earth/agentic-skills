interface:
  display_name: "Resolve Managed Goal Artifacts"
  short_description: "Resolve exact managed goal artifact files"
  default_prompt: "Use $resolve-managed-goal-artifacts to specify the event-root adapter, its legacy boundary, and exact user-path handling."
