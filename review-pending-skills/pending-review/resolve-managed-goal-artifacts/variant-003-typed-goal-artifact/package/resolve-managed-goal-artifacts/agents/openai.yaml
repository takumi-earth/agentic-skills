interface:
  display_name: "Resolve Managed Goal Artifacts"
  short_description: "Resolve exact managed goal artifact files"
  default_prompt: "Use $resolve-managed-goal-artifacts to apply the variant-003-typed-goal-artifact pending design with explicit evidence and authority boundaries."
