interface:
  display_name: "Resolve Managed Goal Artifacts"
  short_description: "Resolve exact managed goal artifact files"
  default_prompt: "Use $resolve-managed-goal-artifacts to specify typed artifact selection for the chosen producer profile and its lifecycle limits."
