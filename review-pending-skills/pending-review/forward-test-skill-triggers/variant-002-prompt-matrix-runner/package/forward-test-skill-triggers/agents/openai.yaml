interface:
  display_name: "Forward Test Skill Triggers"
  short_description: "Evaluate skills without context leakage"
  default_prompt: "Use $forward-test-skill-triggers to generate an isolated prompt matrix and validate externally collected evaluation results."
