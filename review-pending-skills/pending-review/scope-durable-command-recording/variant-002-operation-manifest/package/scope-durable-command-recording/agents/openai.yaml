interface:
  display_name: "Scope Durable Command Recording"
  short_description: "Reconcile manifests with the command recorder"
  default_prompt: "Use $scope-durable-command-recording to reconcile this task-required manifest with the existing recorder's capabilities and evidence boundaries."
