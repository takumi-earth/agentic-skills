interface:
  display_name: "Enforce Semantic Transform Contracts"
  short_description: "Enforce semantic patch contracts"
  default_prompt: "Use $enforce-semantic-transform-contracts to validate a typed semantic transformation declaration without treating schema conformance as behavior proof."
