interface:
  display_name: "Enforce Adaptive Transform Tests"
  short_description: "Reject brittle transform test oracles"
  default_prompt: "Use $enforce-adaptive-transform-tests to make typed structural assertions the only convenient oracle for an adaptive transformation."
