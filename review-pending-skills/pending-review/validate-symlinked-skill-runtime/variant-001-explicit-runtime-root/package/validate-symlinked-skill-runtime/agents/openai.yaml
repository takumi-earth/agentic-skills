interface:
  display_name: "Validate Symlinked Skill Runtime"
  short_description: "Validate scripted skills across deployments"
  default_prompt: "Use $validate-symlinked-skill-runtime to run this stateful target entry point with explicit runtime authority across direct, copied, and linked packages."
