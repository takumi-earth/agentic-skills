{
  "activation_effects": [
    "promotion as a top-level user-level skill",
    "installation or synchronization into a harness skill root",
    "implicit invocation during explicitly authorized delegation"
  ],
  "candidate_name": "orchestrate-codex-forks-and-waits",
  "predecessors": [],
  "provenance": [
    "Session line 219 instruction to use Codex rather than Claude Code subagent workflow",
    "Session line 248 explicit no-full-history override",
    "Session line 371 request for the longest practical wait spans"
  ],
  "relationships": [
    {
      "kind": "overlap",
      "target": "orchestrate-strict-work"
    },
    {
      "kind": "harness-adapter",
      "target": "Codex collaboration tools"
    }
  ],
  "review_notes": [
    "User disposition: retain pending correction as a distinct general Codex adapter whose cross-repository trigger extends beyond `orchestrate-strict-work`. The instruction package remains uncorrected and pending.",
    "The user approved consolidating corrected guidance from `variant-002-wait-budget-protocol` into the optional `orchestrate-strict-work/references/interruptible-worker-waits.md` reference, pruning that draft, and narrowing the owner's rejected-spawn retry rule. Candidate-root Git history preserves the specialized alternative.",
    "Before adoption, distinguish inert translation from delegation execution. Translating a quoted foreign-harness prompt does not authorize launching the workers described inside it.",
    "Honor the exact context request. No prior conversation history requires no inherited history; a small positive turn count is appropriate only when the user permits limited history.",
    "Preserve ownership, budget, concurrency, model, context, and stopping semantics when translating tools. Surface unsupported combinations rather than silently discarding an explicit user requirement. Remove rejected overrides only when they were unnecessary additions by the agent.",
    "Reconcile steering at the actual effect boundary. Interruption of the parent's mailbox wait does not stop a worker; interrupt workers whose continuing effects are no longer authorized. Answer ordinary status questions and resume authorized work without requiring a renewed continue instruction.",
    "Use live tool contracts for timeout selection and returned-state interpretation. A timeout without an update does not itself confirm worker status. Keep interruption, idleness, failure, and completion distinct, and preserve existing integration and wave boundaries. Waiting grants no authority for optional work or additional workers."
  ],
  "schema_version": 1,
  "status": "pending",
  "variant_id": "variant-001-codex-orchestration-adapter"
}
