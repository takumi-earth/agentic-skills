interface:
  display_name: "Audit Architectural Regressions"
  short_description: "Compare architecture across source checkpoints"
  default_prompt: "Use $audit-architectural-regressions to compare architecture across source checkpoints and produce authorized inline or persisted dispositions."
