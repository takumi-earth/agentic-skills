interface:
  display_name: "Plan Strict Work"
  short_description: "Create decision-complete strict work plans"
  default_prompt: "Use $plan-strict-work to create a source-backed, decision-complete plan for this strict-ecosystem task."

policy:
  allow_implicit_invocation: true
