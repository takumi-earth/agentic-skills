# Strict ecosystem ownership model

Use this reference to reason about ownership, not to replace current repository guidance. Verify any drift-prone repository fact locally before applying it.

## Capability test

For every proposed abstraction or edit, answer:

1. What behavior remains if the current caller, file extension, implementation language, and command name disappear?
2. Which invariant must remain true across all consumers?
3. Which repository can enforce that invariant without copying policy into consumers?
4. Which layer produces the data, which layer interprets or converges it, and which layer merely performs operational I/O?
5. Is an apparently unused surface truly obsolete, or is intended wiring incomplete?

Name and place the abstraction around the answers, not around its first caller.

## Layer model

- **Product owner:** owns user-visible capability and public behavior.
- **Policy/model owner:** owns schemas, interpretation, invariants, and convergence.
- **Operational adapter:** performs ecosystem-specific I/O or command integration after policy is resolved.
- **Generator/source owner:** owns generated bytes, fragments, snapshots, or templates.
- **Consumer:** uses an already-owned capability or converged artifact.
- **Harness owner:** owns behavior-observation mechanics, not production policy.

An operational adapter must not absorb cross-language policy merely because it handles the current file. A consumer copy must not become the source of truth merely because a failure appears there.

## Durable decisions evidenced in the trace study

Treat these as working decisions unless current user or repository guidance supersedes them:

- `strict-xtask-core` owns multilingual template-policy interpretation and convergence.
- Cargo- and Bun-specific crates expose operational command surfaces that consume already-converged files.
- `rust-template` is a template surface with minimal local composition, not the default owner of reusable ecosystem engines.
- `agents-md-rs` owns reusable Markdown fragments; generated consumer guidance is changed through its owning fragment/generation workflow.
- Product repositories own their product capabilities. Template automation, policy files, badges, and generated metadata are supporting surfaces unless the repository itself exists to provide them.

## Owner-first stop gates

Stop and investigate before:

- deleting apparently dead or duplicated code;
- creating a parallel helper beside an existing ecosystem abstraction;
- adding or widening a public API, re-export, compatibility shim, or feature;
- editing generated output or copied configuration directly;
- moving behavior because a workspace glob, module tree, diagnostic, or file path makes another location convenient;
- relaxing lint, coverage, mutation, duplication, snapshot, or generated-source policy.

After the owner is established, preserve intended capability, add positive and negative behavior evidence, converge every in-scope consumer, and remove obsolete paths in the same end-state migration.
